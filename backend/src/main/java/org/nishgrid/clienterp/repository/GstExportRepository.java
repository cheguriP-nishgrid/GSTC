package org.nishgrid.clienterp.repository;

import org.nishgrid.clienterp.model.GstExport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GstExportRepository extends JpaRepository<GstExport, Long> {
}